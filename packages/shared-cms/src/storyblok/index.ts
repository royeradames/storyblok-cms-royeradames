/**
 * Storyblok Default Components
 *
 * These are wrappers for Storyblok's default/built-in block types.
 * They allow the Visual Editor to work with content created using
 * Storyblok's default templates.
 */

export { Page, type PageBlok } from "./Page";
export { Teaser, type TeaserBlok } from "./Teaser";
export { Grid, type GridBlok } from "./Grid";
export { Feature, type FeatureBlok } from "./Feature";
